userおすすめ歯磨き粉select画面

<%= search_form_for(@q,url:users_select_user_favorite_path, method: :get,  local:true) do |f| %>
  <%= f.label :tooth_paste_name, "Keyword" %>
  <%= f.search_field :tooth_paste_name_cont %>

  <%= f.label :price %>
  <%= f.number_field :price_gteq %>以上〜
  <%= f.number_field :price_lt %>未満

  <%= f.label :genre_genre_name, "ジャンル名" %>
  <%= f.collection_select :genre_genre_name_eq, @genres, :genre_name, :genre_name, {class: 'form-control'} %>
  <%# genre_genre_name_eq → モデル名_カラム名_eq（ransackの書き方） %>
  <%= f.submit "検索" %>
<% end %>


<% @pastes.each do |paste| %>
<%= link_to(users_new_user_favorite_path(paste)) do %>
<%= attachment_image_tag(paste, :image, size: '100x100') %>
<%= paste.genre.genre_name %>
<%= paste.tooth_paste_name %>
<%= paste.price %>円
<% end %>
<br>
<% end %>
<% if @pastes.present? %>
<%= paginate @pastes %>
<% end %>
<%= link_to "user",users_user_favorite_path(@user_id)	%>