


<div class="main-visual">
<div class="main-visual-content">
    <!-- pタグ（と文字）を追加 -->
    <p class="main-visual-text">
        The world is a book<br>
        and those who do not travel<br>
        read only one page.
    </p>
    </p>
    <p class="main-visual-name">
        St. Augustine
    </p>
</div>
</div>
    <div class="wrapper clearfix">
        <main class="main">総合ランキング
            <section>             
            <% @user_favorites.each_with_index do |favorite, i| %>
                <% if i < 3 %>
                <div class="box_1">
                       <%= i + 1 %><br>
                        <%= attachment_image_tag(favorite[1], :image, size: '150x150') %><br>
                        <%= favorite[0].first %><br>
                        <%= favorite[0].second %><br>
                        <%= link_to "詳しく見る", users_paste_path(favorite[1].id) %>
                <% end %>
                </div>
            <% end %>
            </section>
            <section>ジャンル別:ホワイトニングランキング
                <div class="ranking1">
                <% @user_favorite_genre.each_with_index do |favorite, i| %>
                <% if i < 3 %>
                <div class="box_1">
                         <%= i + 1 %><br>
                        <%= attachment_image_tag(favorite[1], :image, size: '150x150') %><br>
                        <%= favorite[0].first %><br>
                        <%= favorite[0].second %><br>
                        <%= link_to "詳しく見る", users_paste_path(favorite[1].id) %>
                <% end %>
                </div>
            <% end %>
            </section>
        </main>
        <div class="sidemenu">
        <section class="popular">
            <h4 class="sidebar-headline">運営者のおすすめ</h4>
            <div class="sidebar-contents">
                <p class="popular-text">ジャンルごと,ピックアップ</p>
                    <% @admin_recommends.each do |recommend| %>
                        <p><%= recommend.paste.genre.genre_name %></p>
                        <%= attachment_image_tag(recommend.paste, :image, size: '100x100') %>
                        <%= recommend.paste.tooth_paste_name %>
                        <%= recommend.comment %><br>
                        <%= link_to "詳しく見る", users_paste_path(recommend.paste_id) %>
                    <% end %>
            </div>
        </section>
        </div>
    </div>
<%# <div class="box_1">
  box_1
</div>
<div class="box_2">
  box_2
</div>
<div class="box_3">
  box_3
</div> %>
