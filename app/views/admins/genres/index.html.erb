<div class="main-box">
    <div class="container" style="margin:0 50px 0;">
        <div class="row">
            <div class="col-6 text-left">
                    <%= flash[:notice] %>
            </div>
        </div>
        <div class="row">
            <div class="col-3 text-center">
                <h5 class="bg-info text-white rounded p-1 my-5">adminジャンル一覧・追加・編集</h5>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-6">
                <%= form_for(@genre,url:admins_genres_path) do |f| %>
                <%= f.label :genre_name, "ジャンル" %>
                <%= f.text_field :genre_name, autocomplete: "off" %>
                <%= f.submit "追加" %>
                <% end %>
            </div>
            <div class="col-6">
            <% @genres.each do |genre| %>
                <%= form_for(genre, url:admins_genre_path(genre), method: :patch) do |f| %>
                
                <%= f.text_field :genre_name, class:"form-control" %>
                <%= f.submit "変更する" %></td>    
                 <% end %>
                 </tr>
             <% end %>
            </div>
            <%= paginate @genres %>
        </div>
    </div>
</div>

         